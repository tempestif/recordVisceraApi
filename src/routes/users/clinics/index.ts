import { auth } from "@/services/authService";
import express from "express";
const router = express.Router();

// /** 通院記録を取得 */
// router.get("/", auth, readClinicReport)
// /** 通院記録を作成 */
// router.post("/", auth, registClinicReport)
// /** 通院記録を編集 */
// router.post("/edit/:id", auth, editClinicReport)
// /** 通院記録を削除 */
// router.post("/delete/:id", auth, deleteClinicReport)

export { router }
